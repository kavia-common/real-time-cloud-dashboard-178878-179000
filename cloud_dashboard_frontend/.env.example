# Frontend environment variables
# Base URL for HTTP API (Express server)
REACT_APP_API_BASE_URL=http://localhost:4000

# Base URL for Socket.IO (Express server hosting sockets)
REACT_APP_SOCKET_URL=http://localhost:4000

# Optional: custom socket path if backend config uses non-default
# REACT_APP_SOCKET_PATH=/socket.io
